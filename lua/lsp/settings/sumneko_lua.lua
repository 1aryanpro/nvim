return {
  settings = {
    Lua = {
      completion = {
        callSnippet = 'Replace',
      },
      -- diagnostics = {
      --   globals = { 'vim', 'use' },
      -- },
      -- workspace = {
      --   library = {
      --     [vim.fn.expand('$VIMRUNTIME/lua')] = true,
      --     [vim.fn.stdpath('config') .. '/lua'] = true,
      --   },
      -- },
    },
  },
}
